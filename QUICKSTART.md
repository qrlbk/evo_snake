# 🚀 Быстрый старт

## Установка

```bash
cd evo_snake
pip install -r requirements.txt
```

## Запуск

### Минимальный тест (10 поколений)
```bash
python3 main.py --pop 30 --gens 10
```

### Полный прогон (500 поколений)
```bash
python3 main.py --pop 100 --gens 500
```

### Просмотр истории
```bash
# Все сессии
python3 view_history.py

# Детали сессии
python3 view_history.py --session 2

# Лучшие змейки
python3 view_history.py --best
```

### С визуализацией
```bash
python3 main.py --visualize --pop 50 --gens 100
```

Нажмите **SPACE** для перехода к следующему поколению

### Автоматический режим
```bash
python3 main.py --visualize --auto --pop 50 --gens 100
```

## Ожидаемые результаты

С улучшенной системой наград обучение идёт быстрее:

- **Поколение 0-5**: Начало быстрого роста, видно улучшения
- **Поколение 5-30**: Стабильный поиск еды, fitness растёт
- **Поколение 30-100**: Избегание стен, эффективные паттерны
- **Поколение 100+**: Профессиональное поведение, высокая выживаемость

## Финальная демонстрация

После завершения эволюции программа покажет лучшую змейку в действии.

## Структура проекта

- `brain.py` - Нейронная сеть змейки
- `snake.py` - Игровая логика
- `environment.py` - Игровая среда
- `evolution.py` - Генетический алгоритм
- `visualizer.py` - Визуализация pygame
- `database.py` - SQLite база данных
- `view_history.py` - Просмотр истории
- `main.py` - Точка входа

## Советы

- Используйте `--pop 200` для большего разнообразия
- Увеличивайте `--max-steps 1000` для более длинных игр
- `--mutation-rate 0.15` для более быстрой адаптации
- `--grid 15` для более быстрого обучения

