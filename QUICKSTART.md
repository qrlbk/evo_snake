# 🚀 Быстрый запуск

## Одна команда для всего!

### Linux / Mac:
```bash
./run.sh --visualize --auto
```

### Windows:
```cmd
run.bat --visualize --auto
```

### Или напрямую через Python:
```bash
python3 run.py --visualize --auto
```

## Что делает скрипт автоматически:

1. ✅ Проверяет версию Python (нужна 3.7+)
2. ✅ Проверяет и устанавливает зависимости (numpy, pygame)
3. ✅ Создает базу данных если её нет
4. ✅ Запускает программу

## Параметры запуска:

```bash
# С визуализацией и авторежимом
./run.sh --visualize --auto

# С визуализацией, большая популяция
./run.sh --visualize --pop 100 --gens 200

# Без визуализации (быстрее)
./run.sh --pop 50 --gens 100

# Все параметры
./run.sh --visualize --auto --pop 50 --gens 100 --grid 20 --max-steps 100000
```

## Если что-то не работает:

1. Убедитесь что Python 3.7+ установлен: `python3 --version`
2. Установите зависимости вручную: `pip install -r requirements.txt`
3. Запустите напрямую: `python3 main.py --visualize`
