# 🚀 ОДНА КОМАНДА ДЛЯ ЗАПУСКА

## Просто запустите:

### Linux / Mac:
```bash
./run.sh --visualize --auto
```

### Windows:
```cmd
run.bat --visualize --auto
```

### Или через Python:
```bash
python3 run.py --visualize --auto
```

## Что происходит автоматически:

1. ✅ Проверяется версия Python (нужна 3.7+)
2. ✅ Проверяются и устанавливаются зависимости (numpy, pygame)
3. ✅ Создается база данных если её нет
4. ✅ Запускается программа с новым ярким дизайном!

## Примеры команд:

```bash
# С визуализацией и авторежимом (рекомендуется для стрима)
./run.sh --visualize --auto

# Большая популяция, много поколений
./run.sh --visualize --auto --pop 100 --gens 200

# Без визуализации (быстрее для обучения)
./run.sh --pop 50 --gens 100

# Все параметры
./run.sh --visualize --auto --pop 50 --gens 100 --grid 20 --max-steps 100000
```

## Готово! 🎉

Теперь просто запустите одну команду и всё заработает!

